apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: batman-vol
  namespace: chrs
spec:
  storageClassName: rook-block
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 150Gi
