apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: BatmanVol
  namespace: chrs
spec:
  storageClassName: rook-block
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 150Gi
